# Networking Commands in Kali

Commands practiced:
- ip a (check IP address)
- ip route (default gateway)
- ping (connectivity test)
- nslookup (DNS resolution)
- ss (listening ports)

These commands are used for both troubleshooting and security analysis.
