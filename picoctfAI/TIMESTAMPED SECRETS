================================================================================
CHALLENGE WRITEUP: TIMESTAMPED SECRETS (picoCTF 2026)
================================================================================

1. CHALLENGE OVERVIEW
--------------------------------------------------------------------------------
* Category: Cryptography
* Difficulty: Medium
* Vulnerability: Weak Cryptographic Key Generation (Predictable PRNG Source)
* Concept: AES encryption keys must be generated using cryptographically secure 
  random sources. Relying on predictable parameters like the system time (Unix 
  timestamp) collapses the keyspace, allowing for simple brute-force attacks or 
  direct key derivation.


2. VULNERABILITY ANALYSIS
--------------------------------------------------------------------------------
Reviewing the provided source code revealed a critical implementation flaw:

    def encrypt(plaintext: str, timestamp: int) -> str:
        timestamp = int(time.time())
        key = sha256(str(timestamp).encode()).digest()[:16]
        ...

* The script prints out a `timestamp` hint tracking the approximate time of the 
  execution.
* Inside the `encrypt` function, the `timestamp` variable is immediately 
  overwritten with the current time (`int(time.time())`) right before hashing.
* This logic flaw guarantees that the timestamp printed in the hint matches the 
  exact number used to construct the AES-128 key.


3. CRYPTOGRAPHIC DATA
--------------------------------------------------------------------------------
* Public Hint Timestamp : 1770242610
* Target Ciphertext (Hex): 71cd3848348a45b82789f710c3321aceab2171e004200b57fe9cc64d4ea33cec
* Cipher Mode           : AES-ECB (Electronic Codebook)


4. RECOVERY METHODOLOGY (THE MATH)
--------------------------------------------------------------------------------
1. Reconstruct the raw key string using the precise integer value from the hint:
   String: "1770242610"

2. Compute the SHA-256 hash of the string and truncate it to the first 16 bytes 
   (128-bit key size):
   Key = SHA-256(b"1770242610")[:16]

3. Initialize an AES cipher engine in ECB mode using the recovered key.

4. Run the ciphertext through the decryption engine and remove the standard 
   PKCS7 padding to parse the final plaintext flag payload.


5. SOLUTION SCRIPT (PYTHON)
--------------------------------------------------------------------------------
```python
import hashlib
from Crypto.Cipher import AES
from Crypto.Util.Padding import unpad

# Configuration parameters
ts_hint = 1770242610
ct_hex = "71cd3848348a45b82789f710c3321aceab2171e004200b57fe9cc64d4ea33cec"

# 1. Re-derive the key exactly like the server did
key_string = str(ts_hint).encode()
aes_key = hashlib.sha256(key_string).digest()[:16]

# 2. Setup the decryption cipher
cipher = AES.new(aes_key, AES.MODE_ECB)

# 3. Decrypt and strip PKCS7 padding
ciphertext_bytes = bytes.fromhex(ct_hex)
decrypted_bytes = cipher.decrypt(ciphertext_bytes)
flag = unpad(decrypted_bytes, AES.block_size).decode()

print(f"Recovered Flag: {flag}")
```

6. FLAG
--------------------------------------------------------------------------------
picoCTF{sa3S_sEc9t_91609b3c}
================================================================================
