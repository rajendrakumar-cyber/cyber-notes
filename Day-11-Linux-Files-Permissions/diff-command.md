# diff Command

The diff command compares files and shows differences.

Examples:
- diff file1 file2
- diff manuscript_v1.txt manuscript_v2.txt
- diff -r directory1 directory2

Security Value:
SOC analysts use diff to compare logs, configs,
and detect unauthorized changes.
